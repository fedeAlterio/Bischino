<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:forms="clr-namespace:Lottie.Forms;assembly=Lottie.Forms"
             mc:Ignorable="d"
             BackgroundColor="{StaticResource BackgroundBlack}"
             NavigationPage.HasNavigationBar="False"
             x:Class="BischinoTheGame.NameSelectionPage">

    <ScrollView>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="10" WidthRequest="300">
            <Image HorizontalOptions="Center"
                   Source="logo"/>
            <Entry TextColor="White" FontSize="20" HorizontalOptions="Center" HorizontalTextAlignment="Center" PlaceholderColor="Gray" WidthRequest="200" Margin="0,25,0,0"
                   Placeholder="Username" Text="{Binding Player.Name}" MaxLength="16"/>
            <Button HorizontalOptions="Center" Margin="0,20" TextColor="White" BackgroundColor="{StaticResource GoodBlue}" CornerRadius="20"
                    Text="Next" Command="{Binding NextCommand}"/>
        </StackLayout>
    </ScrollView>

</ContentPage>
